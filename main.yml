name: MacRDP
on: 
  workflow_dispatch:
jobs:
  build:
    name: MacRDP
    runs-on: macos-latest
    
    steps:                 
    - name: Connect to Tailscale
      uses: tailscale/github-action@v2
      with:
        # Using your existing secret variable name as requested
        authkey: ${{ secrets.NGROK_AUTH_TOKEN }}
          
    - name: MacOS System running...
      # Upgraded to v3 for better stability
      uses: mxschmitt/action-tmate@v3
      if: always()
